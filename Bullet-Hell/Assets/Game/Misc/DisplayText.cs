using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class DisplayText : MonoBehaviour {

	/*static DamageHandler damageScript;
	//static PlayerSpawn playerScript;

	void OnGUI()
	{
		GUI.Label(new Rect(10, 0, 100, 30), "Health: " + damageScript.healthPoints);
		GUI.Label(new Rect(10, 30, 100, 30), "Lives: " + damageScript.numLives);
	}

	// Use this for initialization
	void Start () {
		damageScript = GetComponent<DamageHandler>();
		//playerScript = GetComponent<PlayerSpawn>();
	}
	
	// Update is called once per frame
	void Update () {
	
	}*/
}
